# deps / build
node_modules/
dist/

# user-specific analysis cache (regenerates from your library)
.cache/

# local settings (contains Spotify client secret)
.config.json

# logs / os
*.log
.DS_Store

# Rust / Tauri build output
app/src-tauri/target/
app/src-tauri/gen/
app/src-tauri/sidecar/

# updater signing key lives in \$HOME, never here — belt & suspenders
*.key
*.key.pub
.bench/
